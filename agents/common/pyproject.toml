[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "olympus-agent-common"
version = "0.12.0"
requires-python = ">=3.11"
dependencies = ["cryptography>=44,<51"]
